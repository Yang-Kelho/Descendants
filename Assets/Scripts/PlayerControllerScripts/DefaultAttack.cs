using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DefaultAttack : MonoBehaviour
{
    public float damage;
    public float speed;
    public DefaultPlayerProjectileScriptableObject defaultProjectile;
 //   GameObject projectile;


    void Update()
    {
        //       if("joystick input goes here")
        //       {
        //           damage = defaultPlayerProjectile.damage;
        //           speed = defaultPlayerProjectile.speed;  //takes data from scriptable object 
        //           GameObject bullet = Instantiate(projectile, transform.position, Quaternion.identity);
        //           Vector2 direction = "joystick direction"
        //           bullet.GetComponent<Rigidbody2D>().velocity = direction * speed;
        //           bullet.GetComponent<EnemyTakeDamage>().damageTaken = damage;
        //       }
        //   
    }
}
